# pneumonia-detection-transfer-learning
Research paper implementation of pneumonia detection from chest X-ray images using pre-trained CNN models (ResNet50, DenseNet, EfficientNet). This project covers dataset preparation, transfer learning, hyperparameter tuning, performance evaluation, and comparison with published results.
